{
    "version": 2,
    "builds": [
      {
        "src": "index.js",
        "use": "@vercel/node",
        "config": {
          "command": "npm install && npm run build"
        }
      }
    ]
  }